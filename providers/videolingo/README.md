# VideoLingo Provider Candidate

[VideoLingo](https://github.com/Huanshere/VideoLingo) is a candidate video
localization provider for subtitle segmentation, translation, alignment, and
dubbing.

The planned adapter will treat the upstream application as an independent
process and normalize its staged output into `video.localize`,
`subtitle.generate`, and `audio.dub` jobs.
